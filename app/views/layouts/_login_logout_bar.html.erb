<%= "Logged in as: " + current_user.username + "(" + current_user.id.to_s + ")" if current_user %>
<% unless current_user %>
  <%= link_to "Login", new_session_url %> |
  <%= link_to "Register", new_user_url %>
<% else %>
  <%= button_to "Logout", session_url, method: :delete %>
<% end %>
